# mock / external-api 모드(VIDEO_LLM_BACKEND=mock|external-api)에서 필요한 최소 런타임
# 의존성입니다. 실제 로컬 모델(torch/transformers 등)은 requirements-real-model.txt에
# 분리되어 있으며, local-model 모드가 아니면 설치하지 않습니다.
#
# app/ 아래 실제 import를 기준으로 구성했습니다 (import 없는 패키지는 포함하지 않음).
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.6.17
fastapi==0.138.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
imageio-ffmpeg==0.6.0
packaging==26.2
pydantic==2.13.4
pydantic_core==2.46.4
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.49.0
