[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not live" --cov=app --cov-report=term-missing --cov-report=xml:coverage.xml --cov-report=html:htmlcov --cov-fail-under=70
markers =
    live: tests that call external paid/free vendor APIs and must be run manually
