# Grafana 데이터소스 프로비저닝: 컨테이너 기동 시 Prometheus가 자동 등록됩니다.
apiVersion: 1

datasources:
  - name: Prometheus
    # 대시보드 JSON에서 이 uid로 참조하므로 바꾸지 마세요.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
